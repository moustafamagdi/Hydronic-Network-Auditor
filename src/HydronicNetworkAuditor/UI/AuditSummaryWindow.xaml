<Window x:Class="HydronicNetworkAuditor.UI.AuditSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hydronic Network Auditor"
        Width="760"
        Height="520"
        MinWidth="680"
        MinHeight="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Hydronic Network Audit" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock x:Name="ModelText" Margin="0,6,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="2" BorderThickness="1" CornerRadius="6" Padding="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="SummaryText"
                         IsReadOnly="True"
                         BorderThickness="0"
                         TextWrapping="NoWrap"
                         FontFamily="Consolas"
                         FontSize="13"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"/>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="FolderText"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis"/>

            <Button Grid.Column="1"
                    Padding="14,7"
                    Content="Open Report Folder"
                    Click="OpenFolder_Click"/>

            <Button Grid.Column="3"
                    Padding="14,7"
                    Content="Close"
                    Click="Close_Click"/>
        </Grid>
    </Grid>
</Window>
